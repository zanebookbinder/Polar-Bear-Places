<h1>Quinby Residents</h1>

<div id="students">
	<% @students.each do |student| %>
			<% if student.Building == "Quinby" %>
			<%= render student %>
			<p>
				<%= link_to "Show this student", student %>
			</p>
			<% end %>
	<% end %>
</div>

<%= link_to "Return home", root_path %>
